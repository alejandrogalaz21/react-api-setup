// Type's
export const {{constantCase name}}_CREATE_REQUEST = '{{constantCase name}}_CREATE_REQUEST'
export const {{constantCase name}}_CREATE_SUCCESS = '{{constantCase name}}_CREATE_SUCCESS'
export const {{constantCase name}}_CREATE_FAILURE = '{{constantCase name}}_CREATE_FAILURE'

export const {{constantCase name}}_READ_MANY_REQUEST = '{{constantCase name}}_READ_MANY_REQUEST'
export const {{constantCase name}}_READ_MANY_SUCCESS = '{{constantCase name}}_READ_MANY_SUCCESS'
export const {{constantCase name}}_READ_MANY_FAILURE = '{{constantCase name}}_READ_MANY_FAILURE'

export const {{constantCase name}}_READ_ONE_REQUEST = '{{constantCase name}}_READ_ONE_REQUEST'
export const {{constantCase name}}_READ_ONE_SUCCESS = '{{constantCase name}}_READ_ONE_SUCCESS'
export const {{constantCase name}}_READ_ONE_FAILURE = '{{constantCase name}}_READ_ONE_FAILURE'

export const {{constantCase name}}_UPDATE_REQUEST = '{{constantCase name}}_UPDATE_REQUEST'
export const {{constantCase name}}_UPDATE_SUCCESS = '{{constantCase name}}_UPDATE_SUCCESS'
export const {{constantCase name}}_UPDATE_FAILURE = '{{constantCase name}}_UPDATE_FAILURE'

export const {{constantCase name}}_REMOVE_REQUEST = '{{constantCase name}}_REMOVE_REQUEST'
export const {{constantCase name}}_REMOVE_SUCCESS = '{{constantCase name}}_REMOVE_SUCCESS'
export const {{constantCase name}}_REMOVE_FAILURE = '{{constantCase name}}_REMOVE_FAILURE'

// Action's
export const {{camelCase name}}CreateRequest = (payload = null) => ({type: {{constantCase name}}_CREATE_REQUEST, payload})
export const {{camelCase name}}CreateSuccess = payload => ({ type: {{constantCase name}}_CREATE_SUCCESS, payload })
export const {{camelCase name}}CreateFailure = (payload = null) => ({type: {{constantCase name}}_CREATE_FAILURE, payload})

export const {{camelCase name}}ReadManyRequest = (payload = null) => ({type: {{constantCase name}}_READ_MANY_REQUEST, payload})
export const {{camelCase name}}ReadManySuccess = payload => ({ type: {{constantCase name}}_READ_MANY_SUCCESS, payload })
export const {{camelCase name}}ReadManyFailure = (payload = null) => ({type: {{constantCase name}}_READ_MANY_FAILURE, payload})

export const {{camelCase name}}ReadOneRequest = (payload = null) => ({type: {{constantCase name}}_READ_ONE_REQUEST, payload})
export const {{camelCase name}}ReadOneSuccess = payload => ({ type: {{constantCase name}}_READ_ONE_SUCCESS, payload })
export const {{camelCase name}}ReadOneFailure = (payload = null) => ({type: {{constantCase name}}_READ_ONE_FAILURE, payload})

export const {{camelCase name}}UpdateRequest = (payload = null) => ({type: {{constantCase name}}_UPDATE_REQUEST, payload})
export const {{camelCase name}}UpdateSuccess = payload => ({ type: {{constantCase name}}_UPDATE_SUCCESS, payload })
export const {{camelCase name}}UpdateFailure = (payload = null) => ({type: {{constantCase name}}_UPDATE_FAILURE, payload})

export const {{camelCase name}}RemoveRequest = (payload = null) => ({type: {{constantCase name}}_REMOVE_REQUEST, payload})
export const {{camelCase name}}RemoveSuccess = payload => ({ type: {{constantCase name}}_REMOVE_SUCCESS, payload })
export const {{camelCase name}}RemoveFailure = (payload = null) => ({type: {{constantCase name}}_REMOVE_FAILURE, payload})

// Reducer

const INITIAL_STATE = {
  one: {},
  many: []
}

export function {{camelCase name}}(state = INITIAL_STATE, action) {
  switch (action.type) {
    case {{constantCase name}}_CREATE_SUCCESS:
      return { ...state, many: [...state.many, action.payload] }
    case {{constantCase name}}_READ_MANY_SUCCESS:
      return { ...state, many: [...action.payload] }
    case {{constantCase name}}_READ_ONE_SUCCESS:
      return { ...state, one: { ...action.payload } }
    case {{constantCase name}}_UPDATE_SUCCESS:
      return {
        ...state,
        many: [...state.many.filter(s => s.id !== action.payload.id), action.payload]
      }
    case {{constantCase name}}_REMOVE_SUCCESS:
      return { ...state, many: [...state.many.filter(s => s.id !== action.payload.id)] }

    default:
      return state
  }
}
